# Colyseus server used by `npm run dev` and production builds (`npm run build`).
# Use your Railway public URL (wss:// or https:// both work with the Colyseus client).
VITE_SERVER_URL=wss://your-app.up.railway.app
